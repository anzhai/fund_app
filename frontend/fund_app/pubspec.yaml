name: fund_app
description: A fund portfolio management app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  dio: ^5.4.0
  intl: ^0.18.1
  fl_chart: ^0.66.0
  cached_network_image: ^3.3.0
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.1.7
  device_info_plus: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
