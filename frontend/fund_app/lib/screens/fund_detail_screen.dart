import 'package:flutter/material.dart';

class PLACEHOLDER_SCREEN extends StatelessWidget {
  const PLACEHOLDER_SCREEN({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SCREEN_TITLE')),
      body: const Center(child: Text('功能开发中')),
    );
  }
}
